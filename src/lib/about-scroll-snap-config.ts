/** Distance from viewport top to section snap line (px). Fine-tune here. */
export const ABOUT_SNAP_HEADER_OFFSET_PX = 40;

export const ABOUT_SNAP_HEADER_OFFSET_CSS_VAR = "--snap-header-offset";

/**
 * Remaining scroll until the document bottom (px). When gap is within this
 * range, footer mode activates and the page snaps to the end. Fine-tune here.
 */
export const FOOTER_END_SNAP_RADIUS_PX = 120;
